# 下一步行动

## 立即可做

1. 初始化 monorepo：`pnpm-workspace.yaml`、`apps/web`、`apps/api`。
2. 搭建 Vite React 前端和基础 Kanban UI。
3. 设计数据库 schema，并选择 Prisma 或 Drizzle。
4. 做一个本地 workspace preview manager 原型。
5. 创建一个 demo workspace 作为被管理项目。

## 第一周目标

- 能创建任务卡片。
- 能启动 demo workspace preview。
- 能在卡片详情页看到 iframe 预览。
- 能记录 timeline event。

## 第二周目标

- 接入一个基础 AI Planning Agent。
- 能根据任务生成执行计划。
- 能生成简单 patch，但先人工确认应用。
- 能运行基本 Playwright 检查。

## 第三周目标

- 自动生成 Before / After 截图。
- 实现 DiffReel 初版。
- 支持 Approve / Request changes。
- 沉淀 Context Pack 初版。

